{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "concurrently \"cd client && npm install\" \"cd backend && npm install\"",
    "run": "concurrently \"cd client && npm run dev\" \"cd backend && npm run dev\"",
    "build": "concurrently \"cd client && npm run build\" \"cd backend && npm run build\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
